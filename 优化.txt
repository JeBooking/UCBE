1、打开插件的时候，就无法对网页进行操作。要将插件的窗口改成浮于当前网页之上，但是不影响网页的操作；
2、加载评论的速度太慢了，只是从数据库中获取保存的数据并在前端展示不应该这么慢，你做一下代码分析，哪里耗时比较长，想想怎么优化，甚至把这块重写我觉得都不为过；
3、没必要专门弄一个页面让填用户名然后保存，点开插件时直接打开页面评论的界面就好了，上面显示评论，下面是要显示的用户名和要发表的评论；
4、在页面评论上增加一个显示信息，显示当前是在对哪个网址添加评论